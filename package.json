{
  "name": "chaojuhuang_personal",
  "version": "0.0.4",
  "homepage": "https://kaddopur.github.io/",
  "private": true,
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "husky": "^0.13.3",
    "lint-staged": "^3.4.1",
    "prettier": "^1.3.0",
    "react-scripts": "0.9.5"
  },
  "dependencies": {
    "classnames": "^2.2.5",
    "react": "^15.2.1",
    "react-dom": "^15.2.1",
    "react-router-dom": "^4.1.1",
    "react-sticky": "^5.0.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build&&gh-pages -d build -b master",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "src/**/*.js": [
      "prettier --single-quote --jsx-bracket-same-line --trailing-comma all --print-width 120 --write",
      "git add"
    ]
  }
}
